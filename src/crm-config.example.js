// Copy this pattern into a deployment-time config file, or inject it before the CRM scripts load.
// Do not place secrets here. This is only the public Worker API base URL.
window.LEAD_API_BASE = "https://YOUR-WORKER.YOUR-SUBDOMAIN.workers.dev";
