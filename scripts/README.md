# CRM import scripts

`import-leads.mjs` uploads a private lead JSON file to the protected Cloudflare Worker import endpoint.

Customer data must stay outside this public GitHub repository. Keep the generated JSON only in a secure local/admin location and remove the Worker import token after the initial migration.
